# ==============================================================
# secure-crm — variables de entorno de ejemplo
# Copia este archivo a .env  (cp .env.example .env)
# ==============================================================

# ---- API (apps/api) ----
API_PORT=4000
JWT_SECRET=secret123

# ---- WEB (apps/web) ----
# URL de la API que consume el frontend (navegador -> API)
NEXT_PUBLIC_API_URL=http://localhost:4000
